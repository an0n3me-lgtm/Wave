{
  "name": "wave-mobile",
  "version": "1.0.0",
  "description": "Wave Messenger — Android",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:web && cap sync android && cap build android",
    "build:web": "npm run build --prefix ../client",
    "sync": "cap sync android",
    "open": "cap open android",
    "run": "cap run android"
  },
  "dependencies": {
    "@capacitor-community/bluetooth-le": "^7.1.0",
    "@capacitor/android": "^7.4.1",
    "@capacitor/app": "^7.0.1",
    "@capacitor/core": "^7.4.1",
    "@capacitor/network": "^7.0.1",
    "@capacitor/splash-screen": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.1",
    "typescript": "^6.0.3"
  }
}
